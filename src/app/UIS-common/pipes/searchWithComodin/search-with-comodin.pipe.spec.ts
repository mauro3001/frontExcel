import { SearchWithComodinPipe } from "./search-with-comodin.pipe";

describe("SearchWithComodinPipe", () => {
  it("create an instance", () => {
    const pipe = new SearchWithComodinPipe();
    expect(pipe).toBeTruthy();
  });
});
