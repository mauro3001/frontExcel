import { CiudadUrlCambioDirective } from "./ciudad-url-cambio.directive";

describe("CiudadUrlCambioDirective", () => {
  it("should create an instance", () => {
    const directive = new CiudadUrlCambioDirective();
    expect(directive).toBeTruthy();
  });
});
